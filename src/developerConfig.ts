/**
 * Developer Configuration
 * Change these values manually for testing and development.
 * This file should not be modified by the app UI.
 */
export const DEV_CONFIG = {
  /**
   * Set to true to bypass all plan locking (locks in the roadmap).
   */
  unlockAllPlans: true,

  /**
   * Set to true to bypass update check throttling and dismissal logic.
   */
  bypassUpdateCache: true,
};
